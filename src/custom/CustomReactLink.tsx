import { Link } from "react-router-dom";
import { styled } from "@gluestack-ui/themed";

// Using the React Link component
const CustomReactLink = styled(Link, {}, {}, {});

export default CustomReactLink;
